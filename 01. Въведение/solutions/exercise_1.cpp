#include <iostream>

int main() {
	/* Декларираме целочислени променливи `a` и `b` */
	int a, b;
	/* Прочитаме стойностите им от клавиатурата */
	std::cin >> a >> b;
	/* Отпечатваме сбора им */
	std::cout << a + b << std::endl;

	return 0;
}